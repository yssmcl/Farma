<!DOCTYPE html>
<html>
<head>
  <title>Farma</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <%= stylesheet_link_tag    'application', :media => 'all' %>
  <%= csrf_meta_tags %>
  <%= javascript_include_tag 'application' %>
</head>
<body data-logged='<%= current_user.to_json %>'>
  <div class="page-container">
    <div id="alert-fixed">
      <span class="loading"><img  class='brand' src='/assets/spinner.gif'> Loading...</span>
    </div>
    <section>
      <header>
        <div class="navbar navbar-inverse navbar-fixed-top">
          <div class="navbar-inner">
            <div class="container-fluid">
              <button type="button" class="btn btn-navbar" id="show_menu_button" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
              <div id="header_left_menu">
              </div>
              <div id="header_right_menu" class="nav-collapse collapse pull-right logged-in">
              </div>
            </div>
          </div>
        </div>
      </header>

      <!-- Subhead ================================================== -->
      <header class="subhead">
        <div class="container-fluid">
          <h1>Ferramenta de Autoria FARMA</h1>
          <p>Ferramenta de Autoria para a Remediação de erros com Mobilidade na Aprendizagem.</p>
          <p>Construa Objetos de Aprendizagem para o ensino de matemática</p>
        </div>
      </header>

      <section class="container-fluid" id="main">
        <%= yield %>
      </section>

    </section>
  </div>

  <div class="modal-container">
        <!-- You can put your modal definitions here but it is not required -->
  </div>

  <script type="text/javascript"
src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.2/MathJax.js">
  </script>
  <script type="text/x-mathjax-config">
	//<![CDATA[
      MathJax.Hub.Config({
	      asciimath2jax: {
      		delimiters: [ ['$','$']]
        },
        tex2jax: {
          inlineMath: [ ['`','`'],["\\(","\\)"] ]
        }
	    });
	 //]]>
  </script>

  <%= render 'layouts/google_analytics' %>
</body>
</html>
